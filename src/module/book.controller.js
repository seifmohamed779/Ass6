import { Router } from "express"
import { add, addBooks, addLog, aggregate1, aggregate2, aggregate3, aggregate4, createAuthors, createBooks, createLogs, deleteBooksBeforeYear, excludeGenres, findBookByTitle, findBooksByGenre, findBooksByYear, findBooksWithIntegerYear, skipLimitBooks, updateBook } from "./book.service"
const booksCases=Router()
booksCases.post("/book",createBooks)
booksCases.post("/authors",createAuthors)
booksCases.post("/logs",createLogs)
booksCases.post("/",add)
booksCases.post("/batch",addBooks)
booksCases.post("logs",addLog)
booksCases.patch("/future",updateBook)
booksCases.get("/find",findBookByTitle)
booksCases.get("/find",findBooksByGenre)
booksCases.get("/find",findBooksByYear)
booksCases.get("/find",findBooksWithIntegerYear)
booksCases.get("/find",excludeGenres)
booksCases.get("/find",skipLimitBooks)
booksCases.delete("/year",deleteBooksBeforeYear)
booksCases.get("/aggregate2", aggregate1)
booksCases.get("/aggregate3", aggregate2)
booksCases.get("/aggregate4", aggregate3)
booksCases.get("/aggregate4", aggregate4)